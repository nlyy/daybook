数据类型的常用方法：
数字类型：
	- int():转换
		int("字段"，base=数字)，数字为进制数：2、8、10、16，默认转换成十进制
		type():获得这个字段的类型
	- bit_length():表示这个数字的二进制位数
		
字符串类型：
	- capitalize():首字母大写
	- casefold():所有的都变小写，很多未知的对相应变小写
	- lower():变小写
	- center(width,可有可无的一个字符):设置宽度，并将内容居中，其他位置用字符代替，默认为空
	- ljust(width,可有可无的一个字符):设置宽度，并将内容居左，其他位置用字符代替，默认为空
	- rjust(width,可有可无的一个字符):设置宽度，并将内容居右，其他位置用字符代替，默认为空
	- zfill(width):设置宽度，并将内容居右，其他位置用0代替
	- count(字符，开始位置，结束位置):寻找字符在字符串中出现的个数，
	- endswidth(字符):检查字符串是否以字符结束。返回值为True或者False
	- startswidth(字符)：检查字符串是否以字符开始。返回值为True或者False
	- find(sub,开始位置，结束位置):默认从前往后找，查找到sub在字符串中的位置，然后就不找了，（大于等于开始位置，小于结束位置），未找到的话返回值为-1
	- index(sub,开始位置，结束位置)默认从前往后找，查找到sub在字符串中的位置，然后就不找了，（大于等于开始位置，小于结束位置），未找到的话直接报错，所以建议一般使用find
	- format():格式化，将一个字符串中的占位符{}替换为指定的值
		test='I am {name}'
		print (test) ----I am {name}
		v = test.format(name='alex');
		print (v)----I am alex
	- format_map()格式化，穿入的值为字典类型
		v1 = test.format_map({name:'alex'})
		print (v1)----I am alex
	- isalnum():判断字符串中是否只包含 字母和数字
	- isalpha():判断字符串中是否只包含 字母和汉字
	- expandtabs(num):以num对字符串进行断开，遇到\t（制表符）就用制表符填充剩余的位置，num 也可以表示每个表格的宽度
	- isdecimal():判断是否是数字，这种②判断结果为False，仅包含十进制的小数，
	- isdigital():判断是否是数字,更优秀，这种②判断结果也是True，包含了十进制的小数和一些特殊符号的数字，但是不支持汉字的数字，例如二
	- isnumeric():判断是否是数字包含了十进制的小数和一些特殊符号的数字，还支持汉字的数字，例如二
	- isprintable():判断字符串中是否包含不可显示或者有特殊意义的字符，例如\t(制表符)、\n（换行符）等等
	- isspace():判断字符串中是否全部都是空格，“”空的结果为False；“   ”结果为True
	- istitle():判断字符串是否是标题
	- join():
		test = "1234567"
		v = "_".join(test)
		print (v)------1_2_3_4_5_6_7
	- islower();是否全是小写
	- lower():转换成小写
	- isupper():是否全是大写
	- upper()：转换成大写
	- strip():去除空格
	- lstrip():去除字符串左侧空格，还能去除换行，例如\t,\n,还能去除字符串中的某个字段，指定了内容，只要有相关的都会去除左侧指定的内容；
	- rstrip():去除字符串右侧空格，还能去除字符串中的某个字段，指定了内容，只要有相关的都会去除左侧指定的内容；
	- maketrans()方法和translate()方法配合使用，
		v = "qeqwebqwvqferqewrqaeaw"
		m = str.maketrans("eq","12");#设置对应关系
		new_v = v.translate(m)#转换
		最后字符串v中的所有'e'都会替换成'1','q'都会替换成'2'
	- partition('字段'):分割的方法。永远只能分割三份，返回元组，字段是可以拿到并成为分割的一部分
	- rpartition('字段')：分割的方法。永远只能分割三份，返回元组，只是从右侧开始寻找，字段是可以拿到并成为分割的一部分
	- split('字段'，数字)：分割的方法，字段是拿不到，数字表示寻找字段的次数，返回列表
	- splitline(True或者False)；默认遇到换行符就分割，且换行符\n不显示，当传值为True时换行符显示，False换行符不显示
	- swapcase():大小写转换
	- replace(被替换的字段，替换的字段，数字):替换，数字表示替换从前到后总共几个

列表：
	修改 
	删除：del
	in 操作		
	
































		
